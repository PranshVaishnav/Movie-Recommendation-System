import {makeStyles} from "@mui/styles";

export default makeStyles(()=>({
    footer : {
        marginTop:"8vh",
        marginBottom:"8vh",
        display:"flex",
        flexDirection:"column",
        alignItems:"center",
        justifyContent:"center",
        color:"white",
    }
}));